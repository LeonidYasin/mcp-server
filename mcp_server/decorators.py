def mcp_tool(func):
    """
    Декоратор для регистрации MCP-инструментов.
    Помечает функцию как MCP-инструмент для автоматического обнаружения.
    """
    func._is_mcp_tool = True
    return func
